apiVersion: v1
kind: Service
metadata:
  name: client-node-port
spec:
  type: NodePort
  ports:
    - port: 9999
      targetPort: 80
      nodePort: 31515
    # - port add the 2nd one in form of an array

  selector:
    component: web
